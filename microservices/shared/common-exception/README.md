# Common Exception Library (`common-exception`)

## Responsibilities
- Uniform global exception handling and API response wrapper definitions across microservices.

## Future Contents
- `ResourceNotFoundException`, `BadRequestException`, `UnauthorizedException`.
- `ErrorResponse` & `ApiResponse` standard models.
- `GlobalExceptionHandler` abstract advice.
